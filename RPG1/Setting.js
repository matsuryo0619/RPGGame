import { canvas, ctx, Width, Height, State, keys } from './imports.js';

export function setting() {
  if (setting.Screen === 'undefined') {
    setting.Screen = 'Name';
  }

  if (setting.Screen = 'Name') {
    ctx.fillRoundedRect(Width / 2, Height / 2, 500, 500, 15, '#444');
  }
};
